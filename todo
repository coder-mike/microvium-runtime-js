- Build should copy the engine version out of microvium.c and into package.json
- Snapshot support in API
- Expose GC API
- Support for passing arguments/return values of:
  - Closures (by handle)
  - Objects
  - Arrays
  - Uint8Array
  - Host functions
- Get rid of the relative import of microvium in tests (before first release)